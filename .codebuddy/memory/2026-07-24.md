# 2026-07-24

- **从 GitHub 取回并合并远程代码**: 解决了 5 个文件共 7 处冲突。远程新增了 `.github/workflows/build-release.yml` 和 `BUILD-GUIDE.md`。合并策略：build-appimage.sh/install-deps.sh/native-modules.sh ripgrep 用远程；electron.sh/native-modules.sh pty合成包 用本地。
- **注意**: GitHub 直连不可用，需通过 `127.0.0.1:7890` (verge-mihomo) 代理访问。

- **修复 5.3.3 构建失败**: `make build-app` 在打补丁阶段因必需补丁 `updateMenuDisabled` 失败而 `make: *** 错误 1`。根因：5.3.3 把菜单项构建函数 `getUpdateMenuItem(texts, updateState)` 从主进程 bundle 移到了渲染进程 bundle，patcher 只改 `main/index.js`，锚点再也匹配不到。功能禁用已由另外两个必需补丁覆盖（`updateRpcDisabled` 基于 `registerUpdateHandlers(registry, deps)`、`updateServiceDisabled` 基于 `UpdateServiceLinux` 早期 return），点击"检查更新"会变成 no-op。修复：把 `updateMenuDisabled` 由 `markRequired` 改为 `markOptional`（与其他可选补丁一致），并补注释。改文件 `scripts/lib/apply-linux-patches.js` 第 730-758 行附近。已用临时副本验证 patcher 退出码 0。注意其余 8 个 ERROR 为可选补丁跳过（mcp/connector/cli/wechatmp/networkDiagnostics 锚点随版本漂移），均不影响构建。
- **验证方法**: `NODE_PATH=<含@electron/asar的node_modules> node scripts/lib/apply-linux-patches.js <app.asar副本> __WB_LINUX_PATCHES_V15__ @lydell/node-pty-linux-loong64` 看是否退出 0。
